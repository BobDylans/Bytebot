# 实现与交付

## 1. MVP 任务拆解（示例）
- 文档导入与解析
- Chunk 规范与元数据
- 向量索引
- 关键词索引
- 混合检索策略
- 生成与引用组装
- API / CLI

## 2. 里程碑计划
- M1：最小闭环
- M2：增量索引 + rerank
- M3：评测体系

## 3. 变更与决策
- 记录关键设计变更（日期、原因、影响）

## 本阶段完成标准
- MVP 可运行并可演示
- 代码结构清晰、可扩展
- 文档与实现一致
## 4. 任务拆解（按模块）

### 4.1 导入与解析
- 支持目录导入（递归、白名单）
- 文件解析：PDF/MD/TXT/DOCX
- 元数据抽取（标题、页码、来源）
- 错误处理与失败统计

### 4.2 切分与索引
- Chunk 规则（段落优先 + 长段再切）
- Chunk 元数据与 hash
- 向量写入（pgvector）
- FTS 写入（tsvector + GIN）

### 4.3 检索与融合
- 向量召回 + FTS 召回
- 线性加权融合与排序
- TopN 选择与去重

### 4.4 生成与引用
- Prompt 设计（答案 + 引用）
- LLM 调用（稳定一致）
- 引用合并与校验

### 4.5 API 与服务层
- REST + SSE 实现
- 任务状态查询
- 错误码与响应结构

### 4.6 简易 Web UI
- 查询输入与流式输出
- 引用展示（简洁版）
- 最小可用交互

### 4.7 运维与日志
- 基本日志（导入/查询/错误）
- 简单健康检查
## 5. 里程碑交付清单（M1/M2/M3）

### M1：最小闭环
- 文档导入（目录 + 格式解析 + OCR）
- 切分与索引（pgvector + FTS）
- 混合检索与线性融合
- LLM 生成 + 简洁引用
- REST + SSE API
- 简易 Web UI
- 基本日志

### M2：工程化增强
- 增量索引（事件 + 定时回退）
- 任务状态机与重试
- 质量分析与简单统计

### M3：评测体系
- 小样本评测集 + 真实回归集
- 指标统计与报告
- 版本对比与回归基线
## 6. M1 任务执行步骤（Spring Boot）

1. 初始化 Spring Boot 项目与基础配置（端口、日志、配置文件），搭建模块骨架与包结构。
2. 数据层：建 Postgres 连接与 `documents/chunks/chunk_embeddings/chunk_fts` 表，完成 `pgvector` 与 FTS 索引初始化脚本。
3. 导入层：实现目录扫描、类型白名单、解析器接口；支持 PDF/MD/TXT/DOCX；对图像型 PDF 走 OCR；输出统一文档/段落结构。
4. 切分与索引：按“段落优先 + 长段切分”生成 chunk，计算 hash，写入 `chunks`、向量与 FTS；记录导入统计。
5. 检索层：实现向量召回与 FTS 召回；做线性融合与去重；输出 TopN 候选。
6. 生成层：接入外部 LLM API，拼接上下文，生成回答与简洁引用。
7. API 层：实现 `/v1/documents:import`、`/v1/query`、`/v1/query:stream`、索引状态接口；返回统一错误码与 trace_id。
8. UI：最小查询页（输入/输出/引用），支持 SSE 流式显示。
9. 验收：跑小样本评测，确认闭环可跑与引用可用。
## 7. M1 子任务清单（按模块 + 验收标准）

### 7.1 基础工程
- 子任务：初始化 Spring Boot 项目、配置管理、日志框架
  - 验收：服务可启动，输出启动日志
- 子任务：环境配置（DB/LLM/OCR）
  - 验收：配置项可从环境变量读取

### 7.2 数据层
- 子任务：建表脚本（documents/chunks/chunk_embeddings/chunk_fts）
  - 验收：脚本可一键创建表与索引
- 子任务：pgvector 与 FTS 索引初始化
  - 验收：向量与 FTS 查询可执行

### 7.3 导入与解析
- 子任务：目录扫描 + 白名单过滤
  - 验收：能列出导入文件清单
- 子任务：PDF/MD/TXT/DOCX 解析
  - 验收：每种格式至少解析 1 个样例
- 子任务：OCR 流程
  - 验收：对图像型 PDF 能提取文本

### 7.4 切分与索引
- 子任务：段落优先切分 + 长段再切
  - 验收：chunk 长度分布合理（无超长）
- 子任务：chunk hash 与元数据
  - 验收：hash 稳定、可复现
- 子任务：写入向量与 FTS
  - 验收：向量/FTS 均可检索

### 7.5 检索与融合
- 子任务：向量召回
  - 验收：查询能返回 TopK chunk
- 子任务：FTS 召回
  - 验收：关键词匹配能返回结果
- 子任务：线性加权融合
  - 验收：融合排序合理，输出 TopN

### 7.6 生成与引用
- 子任务：LLM 调用封装
  - 验收：稳定返回答案
- 子任务：引用生成与合并
  - 验收：引用列表简洁且可追溯

### 7.7 API 与 SSE
- 子任务：导入接口 + 状态查询
  - 验收：任务状态可查询
- 子任务：查询接口（非流式 + SSE）
  - 验收：SSE 流式可用
- 子任务：统一错误码
  - 验收：错误响应带 trace_id

### 7.8 简易 Web UI
- 子任务：查询页 + 流式显示
  - 验收：可展示答案与引用

### 7.9 验收与评测
- 子任务：小样本评测
  - 验收：生成评测报告（表格）